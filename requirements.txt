# AI Sharpness Monitor — requirements
# All free, no paid services needed

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Scheduling
apscheduler==3.10.4

# Database — SQLite (built-in) via SQLAlchemy
sqlalchemy==2.0.30
alembic==1.13.1
# No psycopg2 needed — SQLite is built into Python

# HTTP client
httpx==0.27.0

# Telegram bot (optional)
python-telegram-bot==21.3

# Market data — Binance public endpoint (no API key for OHLCV)
ccxt==4.3.23

# Config
python-dotenv==1.0.1
pydantic-settings==2.2.1

# Logging
structlog==24.1.0

# CLI runner (optional, nice for dev)
rich==13.7.1

# Testing
pytest==8.2.1
pytest-asyncio==0.23.7
