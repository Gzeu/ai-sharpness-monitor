# AI Sharpness Monitor v0.3.0
# $0/month stack: Cerebras free tier + SQLite + Binance public API

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Scheduling
apscheduler==3.10.4

# Database — SQLite (no server, zero config)
sqlalchemy==2.0.30

# HTTP client
httpx==0.27.0

# Telegram bot (optional — only needed if TELEGRAM_BOT_TOKEN is set)
python-telegram-bot==21.3

# Market data — Binance public endpoint, no API key required
ccxt==4.3.23

# Config
python-dotenv==1.0.1
pydantic-settings==2.2.1

# Logging
structlog==24.1.0

# Static file serving (for /dashboard endpoint)
aiofiles==23.2.1

# Dev / lint
rich==13.7.1
ruff==0.4.5

# Testing
pytest==8.2.1
pytest-asyncio==0.23.7

# ML prep (optional — only needed when running scripts/train.py)
# scikit-learn==1.5.0
# numpy==1.26.4
